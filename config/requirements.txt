# ==============================================================================
# MEDIATRENDS — Requirements
# ==============================================================================

# Django
Django>=5.0

# Celery & Redis
celery>=5.3.0
redis>=5.0.0

# HTTP Requests (used by Jina AI Reader + Telegram Bot API)
requests>=2.31.0

# LangChain — document processing and text splitting
langchain>=0.1.0
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1

# Sentence-Transformers — FREE multilingual embeddings
sentence-transformers>=2.3.0
torch>=2.0.0
scikit-learn>=1.3.0

# ElasticSearch — vector search and article indexing (must match server version)
elasticsearch>=8.11.0,<9.0.0

# Utilities
python-dateutil>=2.8.0
tqdm>=4.66.0
numpy>=1.24.0

# Translation (keyword aliases across languages)
deep-translator>=1.11.0
