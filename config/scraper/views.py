# Views not needed — all management is done via Django admin panel.
# Source management, article inspection, and user keyword administration
# are handled entirely through the Django admin interface.